// This component is no longer used — RoomCard logic is inline in Dashboard.tsx
// Keeping this file to avoid import errors from any stale references.
export default function RoomCard() { return null; }
